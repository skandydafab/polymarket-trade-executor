[pytest]
testpaths =
    tests
markers =
    unit: fast isolated unit tests
    integration: cross-subsystem integration tests
    failure_mode: explicit failure-path safety tests
    replay: deterministic journal/replay tests
    property_style: deterministic property-style invariant tests
    performance: lightweight hot-path regression checks
